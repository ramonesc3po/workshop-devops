#!/usr/local/env groovy

/*
  Provision terraform webapp
*/

pipeline {
  agent {
    docker {
      image 'hashicorp/terraform'
    }
  }

  stages {
    stage('build') {
      steps {
        echo build
      }
    }

    stage('plan') {
      steps {
        echo plan
      }
    }
  }
}